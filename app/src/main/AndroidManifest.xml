<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.bupt.runningapp">

    <!-- permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <!-- application -->
    <application
        android:name=".RunnerApplication"
        android:allowBackup="true"
        android:icon="@mipmap/runningman"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Amap -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="8ad9a13316892f585919240ad003d975" />

        <service android:name="com.amap.api.location.APSService" />

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StatisticActivity" />
        <activity android:name=".RunningPreparationActivity" />
        <activity android:name=".UserInfoActivity" />
        <activity android:name=".BodyDataActivity" />
        <activity android:name=".HistoryListActivity" />
        <activity android:name=".HistoryDetailActivity" />
        <activity android:name=".RouteSelectActivity" />
        <activity android:name=".RunningNaviActivity" />
        <activity android:name=".RunningFinishActivity" />
        <activity android:name=".GiftListActivity" />
        <activity android:name=".SearchPassingPointActivity"/>
        <activity android:name=".SearchPassingPointActivity"></activity>
    </application>

</manifest>